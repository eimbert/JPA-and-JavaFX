package com.java.so;

import org.junit.Test;

import com.java.te.JPAUnitTest;

public class ShoppingBasketTest extends JPAUnitTest {

	@Test
	public void testAddProduct() {
		
		/*
		 * Product p = new Product("Prueba A", "A", 30, 5.5); ShoppingBasket sp = new
		 * ShoppingBasket(1); em.getTransaction().begin(); em.persist(p);
		 * sp.addProduct(p, 5);
		 * 
		 * em.persist(sp); em.getTransaction().commit(); //fail("Not yet implemented");
		 */	}

}
